class ListEntry {
    constructor(name, ip, players, maxPlayers, version, type) {
        this.name = name;
        this.ip = ip;
        this.players = players;
        this.maxPlayers = maxPlayers;
        this.version = version;
        this.type = type;
    }
}